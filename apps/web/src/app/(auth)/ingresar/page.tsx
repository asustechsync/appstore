export const metadata = { title: "Ingresar" };

/** F2: formulario validado con `esquemaIngreso` de @appstore/tipos. */
export default function PaginaIngresar() {
  return (
    <main>
      <h1>Ingresar</h1>
    </main>
  );
}
