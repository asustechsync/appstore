export const metadata = { title: "Crear cuenta" };

/** F2: formulario validado con `esquemaRegistro` de @appstore/tipos. */
export default function PaginaRegistro() {
  return (
    <main>
      <h1>Crear cuenta</h1>
    </main>
  );
}
